#!/bin/groovy
pipeline {
  agent any
  stages {
    stage('Cloning Git') {
      steps {
        script {
          git 'https://github.com/vj1200kumar/demoapp.git'
        }
      }
    }  
	stage('Install dependencies') {
      steps {
        sh 'npm install'
      }
    }
    stage('Test') {
      steps {
        script {
          sh 'npm run test'
        }
      }
      post {
        always {
          step([$class: 'CoberturaPublisher', coberturaReportFile: 'coverage/cobertura-coverage.xml'])
        }
      }
    }
  }
}